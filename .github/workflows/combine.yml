name: Combine

on:
  workflow_dispatch:

jobs:
  ASports:
    runs-on: ubuntu-latest
    environment: Cricket

    steps:

      - name: Download remote bash file
        run: wget "${{ secrets.Main }}/ptv.sh"
        
      - name: Show Files
        run: ls
